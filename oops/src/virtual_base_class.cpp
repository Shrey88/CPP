#include "../header/virtual_base_class.h"

/*
 *
 *  Virtual Base Class: 
 *      Virtual base classes are used in virtual inheritance in a way of preventing 
 *      multiple "instances" of a given class appeasring in an inheritance hierarchy when 
 *      using multiple inheritances.
 * 
 */

int main(){

    D d;
    d.show();   

    return 0;
}