#include <iostream>


/*
	Time Complexity: O(squareroot(n))

	here the condition is that :

	i*i < n //that means i*i is nothing but i^2

	i^2 = n	// so have equated the expression here

	i = squareroot(n)
*/
for (int i = 0; i * i < n; i++)
{
	//statement;	//<- this will get executed for root n times
}